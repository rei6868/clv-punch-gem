name: Cyberlogitec Auto Punch (WFH)
on:
  workflow_dispatch: # Cho phép chạy thủ công
  schedule:
    # Chạy lúc 1:00 UTC (8:00 Sáng VN) - Thứ 3, Thứ 4
    - cron: '0 1 * * 2,3'
    # Chạy lúc 10:35 UTC (17:35 Chiều VN) - Thứ 3, Thứ 4
    - cron: '35 10 * * 2,3'

jobs:
  punch-in-out:
    name: Run Punch In/Out Script
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: 1. Check out repository
        uses: actions/checkout@v4

      - name: 2. Set up Node.js v20
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: 3. Install dependencies
        run: npm install

      - name: 4. Install Playwright browsers and dependencies
        run: npx playwright install --with-deps chromium

      - name: 5. Run Playwright test
        run: npx playwright test tests/cyberlogitec.spec.js
        env:
          CYBERLOGITEC_USERNAME: ${{ secrets.CYBERLOGITEC_USERNAME }}
          CYBERLOGITEC_PASSWORD: ${{ secrets.CYBERLOGITEC_PASSWORD }}
          GOOGLE_CHAT_WEBHOOK_URL: ${{ secrets.GOOGLE_CHAT_WEBHOOK_URL }}
          CLOUDINARY_CLOUD_NAME: ${{ secrets.CLOUDINARY_CLOUD_NAME }}
          CLOUDINARY_API_KEY: ${{ secrets.CLOUDINARY_API_KEY }}
          CLOUDINARY_API_SECRET: ${{ secrets.CLOUDINARY_API_SECRET }}
      
      - name: 6. Notify on Workflow Failure
        if: failure() # Chỉ chạy nếu các bước trên bị lỗi (trước khi script test kịp chạy)
        run: |
          curl -X POST -H 'Content-Type: application/json' \
          '${{ secrets.GOOGLE_CHAT_WEBHOOK_URL }}' \
          -d '{ "text": "🚨 **Workflow `Auto Punch (WFH)` đã thất bại.**\nLỗi xảy ra ở bước setup (ví dụ: install dependencies), không phải lỗi do script test.\nCần kiểm tra ngay tại tab Actions của GitHub." }'
